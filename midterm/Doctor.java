class Doctor implements SeesPatients {
  Patient[] patients;

  // implement isPatient method from SeesPatients interface
  public boolean isPatient(Patient patient) {
    // in order to determine if a patient is in the list of patients
    // we need to iterate through the list of patients

  }
}
